<h1><%= t('views.common.title_new', name: Comment.model_name.human.downcase) %></h1>

<%= render "form", comment: @comment %>

<br>

<div>
  <%= link_to t('views.common.show', name: @comment.imageable.model_name.human.downcase), polymorphic_path(@comment.imageable) %>
</div>
