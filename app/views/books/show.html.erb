<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('link_message.edit_this_book'), edit_book_path(@book) %> |
  <%= link_to t('link_message.back_to_books'), books_path %>

  <%= button_to t('link_message.destroy_this_book'), @book, method: :delete %>
</div>
