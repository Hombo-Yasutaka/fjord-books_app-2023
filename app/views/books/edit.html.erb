<h1><%= t('edit.title') %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('link_message.show_this_book'), @book %> |
  <%= link_to t('link_message.back_to_books'), books_path %>
</div>
